#include "include/math_lib.h"

int main(){

    execute_operation(ADD, 100, 200);
    execute_operation(SUB, 100, 200);
    execute_operation(MUL, 100, 200);
    execute_operation(DIV, 100, 200);

    return 0;
}